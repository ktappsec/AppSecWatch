# Copy to .env.local. The WatchTower FastAPI backend the UI talks to.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8099
# Optional: a default API key (Authorization: Bearer). Can also be set at runtime
# via the Settings page (stored in localStorage, which overrides this).
NEXT_PUBLIC_API_KEY=devkey123
